*.local.log
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
.tox/
.DS_Store
Thumbs.db
*.swp
graphify-out/
.worktrees/

# Manifest lock is per-checkout advisory-lock state (Spec 013), never committed.
.spaex.json.lock

# Spec 023 behavior-harness sidecars (per-checkout state, not versioned).
# NOTE: .spaex.md and .spaex/constitution.d/ are versioned content and MUST NOT
# be gitignored; they are the composed constitution and its source fragments.
.spaex/.stale
.spaex/composer.log
